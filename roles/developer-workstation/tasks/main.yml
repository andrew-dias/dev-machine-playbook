---
- include: setup.yml
  tags: [always]

# APT packages
- include: packages.yml
  tags: [packages]

# ZSH
- include: zsh.yml
  tags: [zsh]

# OS config
- include: sysctl.yml
  tags: [sysctl]

# Non-APT packages
- include: sdkman.yml
  tags: [sdkman]
- include: npm.yml
  tags: [npm]

# OS look and feel
- include: xdg.yml
  tags: [xdg]
- include: fonts.yml
  tags: [fonts]

- include: tmux.yml
  tags: [tmux]
- include: nvim.yml
  tags: [nvim]

- include: teardown.yml
  tags: [always]

